# WE FIRST CREATED BACKEND AND FRONTEND FOLDERS


# create virtual environment on mac/windows

# windows
python -m venv ven_name

# to activate your virtual environment
./venv_name/Scripts/activate

# mac
python -m venv ven_name

# activation
source ven_name/bin/activate

# to install dependencies/libraries
pip install -r requirements.txt


python -m train_and_save

python -m app

# open a new terminal

streamlit run app.py


# converting working directory to local directory we run
git init
# moving files to staging
git add . or git add file_names
# send the files to commit area
git commit -m "message"

# establish the connection
# create a folder on github and at the you will find 3 commands
git remote
git branch
git push -u origin main

# when pushing it for the second time, 
git push -u origin main